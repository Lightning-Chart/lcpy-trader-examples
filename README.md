# LightningChart Python Trader Examples

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Copy `license_key.example.txt` to `license_key.txt` and add your LightningChart Trader key in it.
3. Run the example scripts/notebooks in the `examples` folder.
